services:
  postgres:
    image: postgres:16-alpine
    container_name: pbl26-golf-cart-26-db
    environment:
      POSTGRES_DB: agcdb
      POSTGRES_USER: agc
      POSTGRES_PASSWORD: agcpassword
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backend/sql/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - agc-net
    restart: unless-stopped
    mem_limit: 128m
    mem_reservation: 32m
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agc -d agcdb"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    container_name: pbl26-golf-cart-26-backend
    env_file: .env
    environment:
      DATABASE_URL: postgresql://agc:agcpassword@postgres:5432/agcdb
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - agc-net
    restart: unless-stopped
    mem_limit: 256m
    mem_reservation: 64m

  frontend:
    build: ./frontend
    container_name: pbl26-golf-cart-26-app
    ports:
      - "127.0.0.1:9003:80"
    depends_on:
      - backend
    environment:
      PUBLIC_URL: "https://golf-cart-26.microlab.club"
    networks:
      - agc-net
    restart: unless-stopped
    mem_limit: 64m
    mem_reservation: 16m

volumes:
  postgres-data:

networks:
  agc-net:
    driver: bridge
